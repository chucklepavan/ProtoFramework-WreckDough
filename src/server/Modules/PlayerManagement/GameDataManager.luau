local Proto, doNothing, parallelBreakpoint = _G.Proto, _G.doNothing, _G.parallelBreakpoint
local GameDataManager = Proto:serverModule({
    moduleName = "GameDataManager",
})


function GameDataManager:getGameData(player, profile)
    return {
        
    }
end


return GameDataManager