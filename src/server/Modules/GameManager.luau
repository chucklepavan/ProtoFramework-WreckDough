local Proto, doNothing, parallelBreakpoint = _G.Proto, _G.doNothing, _G.parallelBreakpoint
local GameManager = Proto:serverModule({
    moduleName = "GameManager"
})


--test for how you would replicate a kills variable


return GameManager